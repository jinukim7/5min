{
  "name": "better-school-life",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "esbuild src/js/app.js --bundle --outfile=dist/bundle.js --format=iife"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  }
}
